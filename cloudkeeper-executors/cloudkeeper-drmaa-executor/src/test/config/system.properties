# Empty properties file. Instead of modifying this file, simply create a new
# properties file and pass "-DsystemPropertiesFile=/path/to/file" when running
# Maven.

# Example configuration for Grid Engine:
# org.ggf.drmaa.SessionFactory = com.sun.grid.drmaa.SessionFactoryImpl
